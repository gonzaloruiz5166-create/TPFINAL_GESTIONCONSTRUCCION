<Window x:Class="TP2.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Iniciar Sesión" Height="478" Width="815">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Usuario:" Margin="0,5"/>
        <TextBox x:Name="txtUsuario" Grid.Row="1" Height="25"/>

        <TextBlock Grid.Row="2" Text="Contraseña:" Margin="0,10,0,5"/>
        <PasswordBox x:Name="txtPass" Grid.Row="3" Height="25"/>

        <Button x:Name="btnLogin" Grid.Row="4" Content="Iniciar Sesión" Margin="0,20,0,0" 
                Click="btnLogin_Click"/>
    </Grid>
</Window>